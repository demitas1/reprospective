# Web UI環境変数テンプレート
# このファイルを .env にコピーして使用してください
# cp env.example .env

# ================================
# API Gateway設定
# ================================

# API Gateway URL（ローカル開発環境）
# 注意: プロジェクトルートの .env の API_GATEWAY_PORT と一致させること
VITE_API_URL=http://localhost:8800

# 本番環境（Docker内）では以下を使用
# VITE_API_URL=http://api-gateway:8000

# ================================
# フロントエンドエラーロギング設定
# ================================

# エラーロギング全体の有効/無効（デフォルト: false）
# true に設定すると、以下のエラーソースからのログ送信が有効になります
VITE_ENABLE_ERROR_LOGGING=true

# 各エラーソースの個別制御（デフォルト: すべてtrue）
# 特定のソースのみ無効化したい場合は false に設定してください

# React Error Boundary でキャッチされたエラー
VITE_LOG_REACT=true

# React Query（TanStack Query）のクエリ・ミューテーションエラー
VITE_LOG_REACT_QUERY=true

# Axios HTTPリクエストエラー（API通信エラー）
VITE_LOG_AXIOS=true

# グローバルエラーハンドラー（window.addEventListener('error')）
VITE_LOG_GLOBAL=true

# Promise拒否ハンドラー（window.addEventListener('unhandledrejection')）
VITE_LOG_UNHANDLED_REJECTION=true

# ================================
# 使用例
# ================================
#
# 1. すべてのエラーログを有効化（推奨: 開発初期段階）
#    VITE_ENABLE_ERROR_LOGGING=true
#    （個別設定はすべてデフォルトtrue）
#
# 2. Axiosエラーのみログ送信（推奨: API通信デバッグ時）
#    VITE_ENABLE_ERROR_LOGGING=true
#    VITE_LOG_REACT=false
#    VITE_LOG_REACT_QUERY=false
#    VITE_LOG_AXIOS=true
#    VITE_LOG_GLOBAL=false
#    VITE_LOG_UNHANDLED_REJECTION=false
#
# 3. 本番環境（ログ送信無効）
#    VITE_ENABLE_ERROR_LOGGING=false
#
# 4. ブラウザコンソールから動的制御（開発中）
#    環境変数でロギングを有効化した後、ブラウザコンソールで実行:
#    window.__errorLogger.setSourceEnabled('axios', false)
#    window.__errorLogger.getConfig()
#
